{
  "name": "studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev:studio": "pnpm --filter @nolote/studio dev",
    "dev:template-kit": "pnpm --filter @nolote/studio-template-kit dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "build": "turbo run build",
    "format": "turbo run format",
    "clean": "turbo run clean && rimraf node_modules .turbo"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "rimraf": "^6.0.1",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  }
}
